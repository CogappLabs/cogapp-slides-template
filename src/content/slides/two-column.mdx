---
title: Two column
section: Layouts
bg: cream
align: start
docs:
  - https://docs.astro.build/
  - label: Tailwind CSS
    href: https://tailwindcss.com/
notes: |
  `TwoCol` splits a slide evenly by default. Pass `ratio="2-1"` or `"1-2"` to
  weight one side. The panels here carry a background so the widths are
  visible; a real slide would usually have none.
---

import TwoCol from "../../components/TwoCol.astro";

## Two columns

<TwoCol>
  <div class="bg-white p-5 rounded">Even split, the default.</div>
  <div class="bg-white p-5 rounded">Same width as its neighbour.</div>
</TwoCol>

<TwoCol ratio="2-1">
  <div class="bg-white p-5 rounded">`ratio="2-1"` makes this side twice as wide.</div>
  <div class="bg-white p-5 rounded">`"1-2"` flips it.</div>
</TwoCol>

Each column is plain MDX: headings, lists and prose all work inside one.
