---
title: Terminal
section: Components
bg: cream
align: start
notes: |
  Terminal renders a dark window with macOS-style chrome for command output.
  Pass the output as `text` and an optional `title` for the window label.
  The colours are deliberately outside the brand palette, so a terminal looks
  like a terminal on any background.
---

import Terminal from "@/components/Terminal.astro";

## Terminal

<Terminal
  title="claude"
  text={`$ npm run build

  8 page(s) built in 662ms
  Complete!`}
/>

Keep output to a few lines so it stays legible at presentation distance.
