---
title: Code and terminal
section: Components
bg: cream
align: start
notes: |
  Fenced code blocks are highlighted by shiki and sized for the room by
  `.prose-slide`. Tag a block `terminal` and it renders as a terminal window
  instead, so command output needs no component and no import.

  Alt-click anywhere to magnify that spot for the audience (Ctrl-click on
  Linux). Click again, or press Escape, to go back.
---

## Code and terminal

```js
const slides = slideOrder.length;
console.log(`${slides} slides`);
```

```terminal
$ npm run build

23 page(s) built in 903ms
Complete!
```

Alt-click to magnify a spot; click again to reset.
