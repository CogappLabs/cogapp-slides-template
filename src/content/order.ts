// Source of truth for slide order.
// Each entry is the filename slug (filename minus `.mdx`).
// Reorder by moving lines. Add new slides by inserting filename here.
export const slideOrder = [
  "title",
  "section-divider",
  "bullets",
  "quote",
  "two-column",
  "two-col-component",
  "table",
  "terminal",
  "react-island",
  "closing",
] as const;
